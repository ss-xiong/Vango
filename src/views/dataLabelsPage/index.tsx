import * as React from 'react';
import CSSModules from 'react-css-modules';

import styles from './index.scss';

const dataLablesPage = () => (
  <div styleName="page">Data Labels Page2s2</div>
);

export default CSSModules(dataLablesPage, styles);
