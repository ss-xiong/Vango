import * as React from 'react';
import CSSModules from 'react-css-modules';

import styles from './index.scss';

const dataListsPage = () => (
  <div styleName="page">My Data Lists Pagssse2s</div>
);

export default CSSModules(dataListsPage, styles);
